import { styled } from 'styled-components';
import Icon from '../common/Icon';

export default function Layers() {
  return (
    <LayersWrap>
      <Icon name="layerIcon" size="big" />
    </LayersWrap>
  );
}

const LayersWrap = styled.div``;
